declare enum BuildType {
    Generic = "generic",
    Managed = "managed",
    ManagedClient = "managed-client",
    CustomManaged = "custom-managed",
    CustomManagedClient = "custom-managed-client"
}
declare enum Platform {
    Android = "android",
    iOS = "ios"
}
export { BuildType, Platform };
