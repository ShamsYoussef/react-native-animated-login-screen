import { types } from '../../../buildConfig';
import { ManagedAndroidJob } from './job';
declare function prepareJob(config: types.ManagedAndroidBuildConfig, projectUrl: string, projectDir: string): Promise<ManagedAndroidJob>;
export default prepareJob;
