import Joi from '@hapi/joi';
import { BuildType } from '../../../constants';
import { BaseAndroidJob } from '../baseJob';
interface AndroidKeystore {
    keystoreDataBase64: string;
    keystorePath: string;
    keystorePassword: string;
    keyAlias: string;
    keyPassword: string;
}
interface ManagedAndroidJob extends BaseAndroidJob {
    type: BuildType.Managed;
    packageJson: any;
    manifest: any;
    secrets: {
        keystore: AndroidKeystore;
    };
}
declare const ManagedAndroidJobSchema: Joi.ObjectSchema;
export { ManagedAndroidJob, ManagedAndroidJobSchema, AndroidKeystore, };
