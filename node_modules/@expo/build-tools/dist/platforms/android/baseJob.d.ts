import Joi from '@hapi/joi';
import { Platform } from '../../constants';
interface BaseAndroidJob {
    platform: Platform.Android;
    projectUrl: string;
}
declare const BaseAndroidJobSchema: Joi.ObjectSchema;
export { BaseAndroidJob, BaseAndroidJobSchema, };
