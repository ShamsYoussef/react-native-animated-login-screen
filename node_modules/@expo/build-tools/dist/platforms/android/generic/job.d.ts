import Joi from '@hapi/joi';
import { BuildType } from '../../../constants';
import { FileList } from '../../../types';
import { BaseAndroidJob } from '../baseJob';
interface GenericAndroidJob extends BaseAndroidJob {
    type: BuildType.Generic;
    nativeProjectDirectory: string;
    gradleCommand: string;
    artifactPath: string;
    secrets: {
        files: FileList;
    };
}
declare const GenericAndroidJobSchema: Joi.ObjectSchema;
export { GenericAndroidJob, GenericAndroidJobSchema };
