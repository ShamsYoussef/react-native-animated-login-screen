import { types } from '../../../buildConfig';
import { GenericAndroidJob } from './job';
declare function prepareJob(config: types.GenericAndroidBuildConfig, projectUrl: string, projectDir: string): Promise<GenericAndroidJob>;
export default prepareJob;
