import { Credentials } from '../credentials';
interface GymfileOptions {
    nativeProjectDirectory: string;
    outputName: string;
    outputDirectory: string;
}
declare function createIfNotExists(credentials: Credentials, { nativeProjectDirectory, outputName, outputDirectory }: GymfileOptions): Promise<boolean>;
export { createIfNotExists };
