import { types } from '../../../buildConfig';
import { GenericiOSJob } from './job';
declare function prepareJob(config: types.GenericiOSBuildConfig, projectUrl: string, projectDir: string): Promise<GenericiOSJob>;
export default prepareJob;
