import { types } from '../../../buildConfig';
import { ManagediOSJob } from './job';
declare function prepareJob(config: types.ManagediOSBuildConfig, projectUrl: string, projectDir: string): Promise<ManagediOSJob>;
export default prepareJob;
