import Joi from '@hapi/joi';
import { Platform } from './constants';
import * as Android from './platforms/android';
import * as iOS from './platforms/ios';
declare type Job = Android.Job | iOS.Job;
declare const JobSchema: Joi.AlternativesSchema;
declare function prepareJob(platform: Platform, projectUrl: string, projectDir: string): Promise<Job>;
export { Job, JobSchema, prepareJob };
