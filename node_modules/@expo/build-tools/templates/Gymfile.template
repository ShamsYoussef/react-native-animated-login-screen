clean(true)

export_options({
  method: "app-store",
  provisioningProfiles: {
    "{{BUNDLE_IDENTIFIER}}" => "{{PROVISIONING_PROFILE_UUID}}"
  }
})

export_xcargs "OTHER_CODE_SIGN_FLAGS=\"--keychain {{KEYCHAIN_PATH}}\""

output_directory("{{OUTPUT_DIRECTORY}}")
output_name("{{OUTPUT_NAME}}")
