import { bunyan } from '@expo/logger';
import { SpawnResult, SpawnOptions as SpawnAsyncOptions } from '@expo/spawn-async';
declare type SpawnOptions = SpawnAsyncOptions & {
    logger?: bunyan;
    printAsStdoutOnly?: boolean;
};
declare function spawn(command: string, args: string[], _options?: SpawnOptions): Promise<SpawnResult>;
export default spawn;
export { SpawnOptions, SpawnResult };
